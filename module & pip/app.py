#To import another file just say "import" and the name of the file
import useful_tools

#To use a function in that file, call the file then "."
print(useful_tools.roll_dice(10))

#Pip is a program to install python modules and a project manager
#[pip install module_name]